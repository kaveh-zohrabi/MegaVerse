{
  "name": "megaverse",
  "version": "0.1.0",
  "private": true,
  "description": "MegaVerse - One of the largest modular software ecosystems",
  "workspaces": [
    "apps/*",
    "services/*/src",
    "libraries/*",
    "sdk/javascript",
    "gateway"
  ],
  "scripts": {
    "dev": "next dev --workspace apps/web",
    "build": "next build --workspace apps/web",
    "start": "next start --workspace apps/web",
    "lint": "next lint --workspace apps/web",
    "test": "echo 'No tests yet'"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
